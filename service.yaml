apiVersion: v1
kind: Service
metadata:
  name: homecare-backend-service
  labels:
    app: homecare-backend
spec:
  type: LoadBalancer
  loadBalancerSourceRanges:
    - 203.0.113.10/32
  selector:
    app: homecare-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
